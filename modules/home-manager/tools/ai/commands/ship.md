---
description: Publish repository changes using the configured ship workflow
---

You are running the `/ship` command. Load the `ship` skill, then execute its
workflow end-to-end for the current repository.

Ticket when applicable: `$1`

Additional context and arguments:

$ARGUMENTS
